# Test report

Generated by `npm run test:report`. Do not edit by hand - rerun it.

| | |
| --- | --- |
| Run | 2026-08-31 23:04 UTC |
| Branch | `main` at `3f54ff9` |
| Node | `v22.23.2` |
| Regressions | none |
| Audit findings | 29 |
| Suites that never loaded | none |
| Fuzz-only findings | **1** |

## Lanes

| Lane | Question | Tests | Passed | Failed | Verdict |
| --- | --- | ---: | ---: | ---: | --- |
| Baseline | Did I break something that used to work? | 465 | 465 | 0 | green |
| Audit · server | What does the control plane do that its docs deny? | 124 | 102 | 22 | 22 findings |
| Audit · web | What does the browser client do that its docs deny? | 31 | 24 | 7 | 7 findings |
| Performance | What does each guarantee cost, and is it the shape we think? | 11 | 11 | 0 | green |
| Fuzz campaign | What survives 200 cases but not 20,000? | 55 | 46 | 9 | 9 red, 1 new |

## Performance - what each guarantee costs

Single machine, single process, on whatever hardware ran this. Useful for "is this the shape we think it is" and "did this get an order of magnitude worse". Not a production SLO: there is no network here, no container engine, and one Node process.

| Measurement | p50 | p95 | max | Budget | |
| --- | ---: | ---: | ---: | ---: | --- |
| `concord.doc.read` | 0.896ms | 3.586ms | 12.227ms | 30ms | ok |
| `concord.merge3` | 92.668ms | 130.872ms | 135.272ms | 300ms | ok |
| `concord.provenance` | 46.951ms | 81.131ms | 85.296ms | 300ms | ok |
| `concord.section-check` | 49.261ms | 99.843ms | 120.861ms | 300ms | ok |
| `concord.write.contended` | 1.248ms | 1.796ms | 2.706ms | 250ms | ok |
| `concord.write.large` | 71.618ms | 125.869ms | 137.967ms | 400ms | ok |
| `concord.write.large-allocated` | 110.747ms | 142.743ms | 175.797ms | 500ms | ok |
| `concord.write.small` | 1.396ms | 3.286ms | 13.058ms | 15ms | ok |
| `live.board.poll` | 0.654ms | 1.542ms | 8.117ms | 40ms | ok |
| `live.fanout.50-watchers` | 0.006ms | 0.013ms | 0.215ms | 2ms | ok |

### What the numbers mean

**`concord.doc.read`** - Reading a shared document through the warrant, over HTTP.

> A read takes the same per-document lock as a write, so it is on the queue behind any commit in flight — that is what makes a read consistent with the version it reports rather than a torn view. The number here is the uncontended cost, and it is the one the editor pays when a human opens a file.

<sub>200 runs after warm-up · p50 0.896ms · p95 3.586ms · max 12.227ms · budget 30ms</sub>

**`concord.merge3`** - The three-way merge that makes two Agents' edits both survive.

> Called only when a write arrives against a version that already moved — the interesting case, not the common one. Measured on three genuinely different versions, because with `theirs` unchanged the function returns early and reports a number forty times better that describes the early return rather than the merge. It is the same LCS shape as the diff, run twice (base→ours, base→theirs), so it is the most expensive single step here.

<sub>40 runs after warm-up · p50 92.668ms · p95 130.872ms · max 135.272ms · budget 300ms</sub>

**`concord.provenance`** - Per-line attribution, recomputed inside every accepted commit.

> This is what makes `git blame` per Agent possible, and it is not free: it aligns the old and new line arrays on every commit so an untouched line keeps its author. Being inside the critical section is deliberate — attribution computed after the fact could disagree with what was committed, and a review comment would then be routed at the wrong Agent.

<sub>40 runs after warm-up · p50 46.951ms · p95 81.131ms · max 85.296ms · budget 300ms</sub>

**`concord.section-check`** - Refusing a write that reaches outside the Agent's allocated section.

> The newest thing on the write path, so worth its own number rather than being hidden in the total. It diffs the document to find which lines a write CHANGES — the payload is always the whole file, so the question can only be answered from the diff. That makes it the same order as provenance, and it means an accepted write pays the LCS cost twice. Measured, that is still comfortably inside budget; if it ever stops being, the two share an input and could share one diff.

<sub>40 runs after warm-up · p50 49.261ms · p95 99.843ms · max 120.861ms · budget 300ms</sub>

**`concord.write.contended`** - Eight Agents committing to one document at once — the no-lost-update claim, timed.

> Eight writers race one document and every one of them lands: the store queues them per document, so the last writer waits for seven predecessors and the batch still finishes in well under the budget. This is the cost of the guarantee, and it is the right trade — the alternative to queueing is a lost update. Note what is NOT claimed: this is one process. CONCORD's serialization is a promise chain in memory, so a second server instance would not see it. Single-process is a documented constraint, not an oversight this number hides.

<sub>30 runs after warm-up · p50 1.248ms · p95 1.796ms · max 2.706ms · budget 250ms</sub>

**`concord.write.large`** - A write to a ~1300-line document — well past anything the demo holds.

> This is the one worth watching. The diff underneath provenance and the section check is a longest-common-subsequence table, which is O(n·m) in lines — so twenty times the document is roughly four hundred times the table. The measured p95 confirms that shape rather than contradicting it. It is acceptable HERE because a write is serialized per document and sits inside a model turn measured in seconds, so it is never the thing a human waits for. It would stop being acceptable on a source tree, and the fix then is a windowed diff around the changed hunk rather than the whole file.

<sub>40 runs after warm-up · p50 71.618ms · p95 125.869ms · max 137.967ms · budget 400ms</sub>

**`concord.write.large-allocated`** - The same large write, but with a section allocated — so the ownership check runs.

> The honest cost of section ownership, and the reason it is measured separately: an unallocated document short-circuits the check entirely, so the cheaper number above says nothing about the feature. Against that number this is roughly double, which is exactly right — the write now diffs the document twice, once to decide whether the change is allowed and once to attribute it. Paying twice for a guarantee this strong is a good trade at these sizes, and the two calls share an input, so collapsing them into one diff is available if a bigger document ever makes it matter.

<sub>30 runs after warm-up · p50 110.747ms · p95 142.743ms · max 175.797ms · budget 500ms</sub>

**`concord.write.small`** - A write to a ~60-line document — the size the demo uses.

> Every accepted write runs an authority check, a full-document diff for provenance, and a persistence hop, all inside the per-document critical section. At this size that is sub-millisecond, which matters because the section check and provenance BOTH diff the whole file: the cost is paid twice and is still nothing. The budget is generous against the measured value on purpose — it exists to catch an order-of-magnitude regression, not to pin a laptop's number.

<sub>300 runs after warm-up · p50 1.396ms · p95 3.286ms · max 13.058ms · budget 15ms</sub>

**`live.board.poll`** - One /api/live/board request — what every open browser asks for every two seconds.

> The board holds no state of its own: it composes sessions from the orchestrator, roles from the Registry's warrants, conflicts and presence from CONCORD, and usage from the bus, on every request. That is a deliberate trade — one source of truth per fact, at the cost of recomputing them — and this number is what says the trade is affordable. At a fortieth of the two-second poll interval, a browser spends under two percent of its polling budget here, so caching would buy nothing and could serve a stale conflict count.

<sub>200 runs after warm-up · p50 0.654ms · p95 1.542ms · max 8.117ms · budget 40ms</sub>

**`live.fanout.50-watchers`** - Publishing one Agent event to 50 connected browsers.

> This runs on the Agent's own thread of control: the tap sits in `inspect`, which the runner calls for every line Codex emits, so a slow fan-out would slow the AGENT down rather than the UI. At microseconds for fifty subscribers that is not a risk, and the design keeps it that way — a subscriber that throws is dropped rather than retried, and nothing here awaits anything.

<sub>500 runs after warm-up · p50 0.006ms · p95 0.013ms · max 0.215ms · budget 2ms</sub>

## Audit · server - 22 finding(s)

Red on purpose. Each assertion quotes a line in `docs/` that the
code contradicts, so do not resolve one by weakening the assertion.

### `apps/server/src/client-contract.test.ts`

- **the browser client and the server agree on the API › keeps its unreachable server routes to the documented set**
  <br>`AssertionError: expected { undocumented: [ …(2) ], …(1) } to deeply equal { undocumented: [], stale: [] }`
### `apps/server/src/security.test.ts`

- **route census: every /api route is behind a credential (THREAT_MODEL TB-0) › leaves no route reachable without any credential at all**
  <br>`AssertionError: expected [ …(16) ] to deeply equal []`
- **unauthenticated reader (THREAT_MODEL AC-8, T5) › cannot ask the PDP to authorize an action for an agent it merely names**
  <br>`AssertionError: expected 200 to be 401 // Object.is equality`
- **one human cannot drive another human's agent (WB-6, AC-7) › refuses Alice's agent a write to a file only Bob's subtask owns**
  <br>`AssertionError: expected 401 to be 403 // Object.is equality`
- **one human cannot drive another human's agent (WB-6, AC-7) › does not answer authorization questions about an agent the caller does not own**
  <br>`AssertionError: expected 200 to be 403 // Object.is equality`
- **a warrant grants exactly what it names (WB-5) › does not turn a root sharedPath into a grant over the whole repo**
  <br>`AssertionError: expected 200 to be 403 // Object.is equality`
- **a warrant grants exactly what it names (WB-5) › never maps a shareable docId onto the bare whole-repo prefix (WB-5)**
  <br>`AssertionError: expected 'repo:' not to be 'repo:' // Object.is equality`
- **audit visibility › T7: a human keeps seeing their own decision after 500 unrelated ones**
  <br>`AssertionError: expected +0 to be 1 // Object.is equality`
- **audit visibility › T7: says so when the viewer's window hides decisions it still retains**
  <br>`AssertionError: expected true to be false // Object.is equality`
### `apps/server/src/aegis/latch-toctou.contract.test.ts`

- **§4.7 TOCTOU: re-check immediately before spawn › also releases the concurrency slot admission acquired**
  <br>`AssertionError: expected 1 to be +0 // Object.is equality`
### `apps/server/src/concord/audit-scope.contract.test.ts`

- **the audit chain records authorization, not concurrency outcomes › drives a real merge and a real conflict, then checks what actually reached plane.audit**
  <br>`AssertionError: expected [ { …(10) }, { …(10) } ] to have a length of +0 but got 2`
### `apps/server/src/concord/merge.property.test.ts`

- **splitLines / joinLines round-trip › splitLines(joinLines(lines)) reconstructs the line array**
  <br>`Error: Property failed after 1 tests`
- **splitLines / joinLines round-trip › REGRESSION: a single blank line does not collapse to zero lines**
  <br>`AssertionError: expected [] to deeply equal [ '' ]`
- **splitLines / joinLines round-trip › REAL FINDING: a disjoint merge that reduces to one blank line loses it (merge.ts:131 'Disjoint edits converge')**
  <br>`AssertionError: expected [] to deeply equal [ '' ]`
### `apps/server/src/concord/provenance.property.test.ts`

- **cross-cutting: a surviving lone blank line silently loses its provenance › REAL FINDING: deleting down to one surviving blank line loses that line's provenance with no throw (provenance.ts:100-102's splitLines(nextContent))**
  <br>`AssertionError: expected [] to have a length of 1 but got +0`
- **cross-cutting: a surviving lone blank line silently loses its provenance › REAL FINDING: the same collapse reached through merge3's own pinned blank-line-loss case**
  <br>`AssertionError: expected [] to have a length of 1 but got +0`
### `apps/server/src/review/outcomes.contract.test.ts`

- **review outcomes match the documented CONCORD -> comment status table › denied -> comment stays open, per "`denied` | `denied` | stay `open`"**
  <br>`AssertionError: expected 'failed' to be 'open' // Object.is equality`
- **review outcomes match the documented CONCORD -> comment status table › leased -> comment stays open, per "`leased` | `leased` | stay `open`"**
  <br>`AssertionError: expected 'failed' to be 'open' // Object.is equality`
### `apps/server/src/aegis/policy/totality.property.test.ts`

- **§5.1 Total: "a malformed or unknown request denies, never crashes" › never throws for an arbitrary malformed or unknown PolicyRequest-shaped object**
  <br>`Error: Property failed after 1 tests`
- **§5.1 Total: "a malformed or unknown request denies, never crashes" › REAL FINDING, pinned: a request with no context at all crashes evaluate() today**
  <br>`AssertionError: expected [Function] to not throw an error but 'TypeError: Cannot read properties of …' was thrown`
- **§5.1 Total: "a malformed or unknown request denies, never crashes" › REAL FINDING, pinned: a request with context explicitly null crashes evaluate() today**
  <br>`AssertionError: expected [Function] to not throw an error but 'TypeError: Cannot read properties of …' was thrown`
### `apps/server/src/aegis/sandbox/topology.contract.test.ts`

- **§4.4 doc claim: the bridge is created with --internal › creates the network with "--internal" in the docker network create argv**
  <br>`AssertionError: expected last "vi.fn()" call to have been called with [ 'docker', …(1) ]`

## Audit · web - 7 finding(s)

Red on purpose. Each assertion quotes a line in `docs/` that the
code contradicts, so do not resolve one by weakening the assertion.

### `apps/web/src/api.test.ts`

- **credential routing › documents that a concord read route (docs) deliberately uses the shared token, not the session token**
  <br>`AssertionError: expected 'Bearer session-token-for-alice' to be 'Bearer shared-demo-token' // Object.is equality`
- **credential routing › REAL FINDING: an identity route silently falls back to the shared token when no human has signed in**
  <br>`AssertionError: expected 'Bearer shared-demo-token' not to be 'Bearer shared-demo-token' // Object.is equality`
### `apps/web/src/console.test.tsx`

- **Console › shows a sign-in prompt and a WAITING chain state before anything has loaded**
  <br>`TestingLibraryElementError: Unable to find an element with the text: WAITING. This could be because the text is broken up by multiple elements. In this case, you can provide a function for your text matcher to make your matcher more flexible.`
- **Console › does not crash when /api/warrant/humans responds without a humans field**
  <br>`Error: Unable to find an element with the text: authorization record. This could be because the text is broken up by multiple elements. In this case, you can provide a function for your text matcher to make your matcher more flexible.`
- **Console › surfaces a refresh failure as a visible error banner once a human is signed in**
  <br>`Error: Unable to find role="button" and name "Alice"`
### `apps/web/src/review.test.tsx`

- **ReviewPanel › shows a loading skeleton while review state has not loaded yet**
  <br>`TestingLibraryElementError: Unable to find a label with the text of: Loading review comments`
- **ReviewPanel › shows an empty-state hint once state has loaded with no comments**
  <br>`TestingLibraryElementError: Unable to find an element with the text: /no comments yet\. select a document line/i. This could be because the text is broken up by multiple elements. In this case, you can provide a function for your text matcher to make your matcher more flexible.`

## Fuzz campaign - 1 finding(s)

Found by the campaign and NOT by the pinned lane, so each of these
is a lead rather than a reproducible finding: 8 other failure(s) here are the audit
lane's, at a higher case count. Pin the seed fast-check printed as a
property in the audit lane before treating one as fixed.

### `apps/server/src/warrant/resources.property.test.ts`

- **normalisePath never escapes upward › a leading run of '..' segments (more than the path is deep) is absorbed, not carried through**
  <br>`Error: Property failed after 955 tests`
  <br>`{ seed: 628599405, path: "954:0:17:12:12", endOnFailure: true }`
  <br>`Counterexample: [0,[".. "]]`

## Not covered

Stated so the totals above are not read as coverage they are not.

- The onboarding tour has no automated test. Verified by hand only.
- The share UI has no automated test; the server half of sharing does.
- The fuzz lane is seedless, so its result is this run's sample and
  will not reproduce. Only the audit lane's pinned properties will.
