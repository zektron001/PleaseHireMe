node_modules/
dist/
.env
.env.production
.data/
.local/
data/
workspaces/
codex-home/
coverage/
*.log
*.tsbuildinfo
.DS_Store
deploy/volcengine/.terraform/
deploy/volcengine/*.tfstate*
deploy/volcengine/terraform.tfvars
.claude

# AEGIS protected asset (Track C). Never commit the demo vault.
vault/
.gstack/

# Generated test runs. The Markdown report is committed; the raw
# vitest JSON beside it is run output and is not.
artifacts/tests/*.json
artifacts/tests/REPORT.partial.md

# The performance lane writes here from whichever cwd invoked it; the file is
# anchored to the repo root now, but a stray workspace copy is still possible.
apps/*/artifacts/
